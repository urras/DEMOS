

             Архив сформатированных документов
                 операционной системы ДЕМОС
           Имена файлов и названия документов
           (в скобках указано число страниц).
          Для печати на АЦПУ используйте команду:
                print -2 -t -w140 имя-файла


as.lp     - Описание языка ассемблер(18).

bcdc.lp   - Программы для  вычислений  с  произвольной  точ-
            ностью - BC и DC (28).

c.lp      - Описание языка си (91).

cdeb.lp   - Диалоговый отладчик для языков си и фортран-77
            (16).

csh.lp    - Интерпретатор команд CSHELL (91).

curses.lp - Библиотека программ для работы с экраном  терми-
            нала (libcurses) (29).

ed.lp     - Редактор текстов ED (43).

f4.lp     - Описание языка фортран-4 (100).

f77.lp    - Описание языка фортран-77 (143).

f77p2.lp  - Фортран-77. Руководство программиста (37).

fsck.lp   - Программа проверки файловой системы (29).

impl.lp   - Описание применения (25).

kp.lp     - Контрольный пример (8).

learn.lp  - Обучающая система LEARN (11).

lex.lp    - LEX - генератор программ лексического анализа
            (40).

m4.lp     - Макропроцессор M4 (14).

mxbook.lp - форматирование документов с помощью пакета "mx".

oper.lp   - Руководство оператора (75).

pif.lp    - Система подготовки документации PIF (51).

red.lp    - Универсальный полиэкранный редактор текстов (50).

rp.1.lp   - Руководство программиста, часть  1.  Введение  в
            систему (25).

sed.lp    - Редактор текстов SED (17).

sh.lp     - Командный язык SHELL (37).

sys.lp    - Руководство системного программиста (84).

yacc.lp   - Компилятор компиляторов YACC (42).
