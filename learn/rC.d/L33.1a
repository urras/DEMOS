#print
Напишите программу, которая подсчитывает число своих
аргументов, начинающихся с буквы 'b'.
А результат печатает в десятичной форме.
И как обычно,
скомпилируйте, проверьте, прогоните, наберите .....
#user
a.out abc bcd efg rpq b bbvd  >xxx
grep 3 xxx >/dev/null
#succeed
/*  возможное решение */
main(argc, argv)
char *argv[];
{
	int i, k;

	for(i=k=0; i<argc; i++)
		if (argv[i][0] == 'b')
			k++;
	printf("%d\n", k);
}
#log
#next
35.1a 10
