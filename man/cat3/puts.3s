
PUTS(3S)        ДЕМОС. Библиотечные функции         PUTS(3S)


ИИММЯЯ
       ppuuttss, ffppuuttss - запись строки в файл.

ФФООРРММААТТ
       #iinncclluuddee <_s_t_d_i_o._h>

       ppuuttss(_s)
       cchhaarr *_s;

       ffppuuttss(_s, _f_i_l_e)
       cchhaarr *_s;
       FFIILLEE *_f_i_l_e;

ООППИИССААННИИЕЕ
       Функция ppuuttss записывает  в  стандартный  файл  вывода
       ssttddoouutt  завершающуюся  нулевым  символом  строку  _s и
       добавляет символ новой строки.

       Функция ffppuuttss записывает в указанный файл _f_i_l_e завер-
       шающуюся нулевым символом строку _s.

       Ни одна из этих функций не записывает в выводной файл
       завершающий нулевой символ.

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _f_o_p_e_n(3s),    _g_e_t_s(3s),     _p_u_t_c(3s),     _p_r_i_n_t_f(3s),
       _f_e_r_r_o_r(3s), _f_r_e_a_d(3s)

































                            -1-                     ДЕМОС/PV

