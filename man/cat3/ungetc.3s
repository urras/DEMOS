
UNGETC(3S)      ДЕМОС. Библиотечные функции       UNGETC(3S)


ИИММЯЯ
       uunnggeettcc - поместить символ в файл ввода.

ФФООРРММААТТ
       #iinncclluuddee <_s_t_d_i_o._h>

       uunnggeettcc(_c, _s_t_r_e_a_m)
       FFIILLEE *_s_t_r_e_a_m;

ООППИИССААННИИЕЕ
       Функция uunnggeettcc помещает символ _c обратно  во  входной
       поток  _s_t_r_e_a_m.  Этот символ будет выдан при следующем
       вызове ggeettcc для этого потока. Функция uunnggeettcc  возвра-
       щает _c.

       Возврат одного символа обратно в поток происходит при
       условии,  что  из  потока  уже  было что-то считано и
       поток на самом деле буферизован. Попытки вернуть  EEOOFF
       (код конца файла) отвергаются.

       Функция ffsseeeekk стирает всю память возвращенных обратно
       символов.

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _g_e_t_c(3s), _s_e_t_b_u_f(3s), _f_s_e_e_k(3s)

ДДИИААГГННООССТТИИККАА
       Функция uunnggeettcc возвращает  EEOOFF,  если  она  не  может
       поместить символ обратно в поток.































                            -1-                     ДЕМОС/PV

