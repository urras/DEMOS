
TTYNAME(3)      ДЕМОС. Библиотечные функции       TTYNAME(3)


ИИММЯЯ
       ttttyynnaammee, iissaattttyy, ttttyysslloott - определить имя терминала.

ФФООРРММААТТ
       cchhaarr *ttttyynnaammee(_f_i_l_d_e_s);

       iissaattttyy(_f_i_l_d_e_s);

       ttttyysslloott();

ООППИИССААННИИЕЕ
       Функция ttttyynnaammee возвращает указатель на завершающееся
       нулевым  символом полное имя специального файла (тер-
       минала), связанного с дескриптором файла _f_i_l_d_e_s.

       Функция iissaattttyy возвращает 1,  если  _f_i_l_d_e_s  связан  с
       терминальным устройством, иначе 0.

       Функция ttttyysslloott возвращает номер записи в файле  _t_t_y_s
       для управляющего терминала текущего процесса.

ФФААЙЙЛЛЫЫ
       /_d_e_v/*
       /_e_t_c/_t_t_y_s

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _i_o_c_t_l(2), _t_t_y_s(5)

ДДИИААГГННООССТТИИККАА
       Функция ttttyynnaammee возвращает нулевой указатель  (NNUULLLL),
       если _f_i_l_d_e_s не относится к терминальному устройству в
       каталоге /_d_e_v.

       Функция ttttyysslloott возвращает 0, если /_e_t_c/_t_t_y_s недосту-
       пен или если нельзя определить управляющий терминал.

ЗЗААММЕЕЧЧААННИИЯЯ
       Возвращаемое значение указывает на  статические  дан-
       ные,  содержимое  которых перезаписывается при каждом
       вызове.




















                            -1-                     ДЕМОС/PV

