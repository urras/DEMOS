
STRING(3)       ДЕМОС. Библиотечные функции        STRING(3)


ИИММЯЯ
       ssttrrccaatt, ssttrrnnccaatt, ssttrrccmmpp, ssttrrnnccmmpp, ssrrttllccmmpp,  ssttrrnnllccmmpp,
       ssttrrccppyy, ssttrrnnccppyy, ssttrrlleenn, iinnddeexx, rriinnddeexx, ttaabbiiffyy - опе-
       рации со строками.

ФФООРРММААТТ
       ##iinncclluuddee <ssttrriinnggss..hh>

       cchhaarr *ssttrrccaatt(_s_1, _s_2)
       cchhaarr *_s_1, *_s_2;

       cchhaarr *ssttrrnnccaatt(_s_1, _s_2, _n)
       cchhaarr *_s_1, *_s_2;

       iinntt ssttrrccmmpp(_s_1, _s_2)
       cchhaarr *_s_1, *_s_2;

       iinntt ssttrrnnccmmpp(_s_1, _s_2, _n)
       cchhaarr *_s_1, *_s_2;

       iinntt ssttrrllccmmpp(_s_1, _s_2)
       cchhaarr *_s_1, *_s_2;

       iinntt ssttrrnnllccmmpp(_s_1, _s_2, _n)
       cchhaarr *_s_1, *_s_2;

       cchhaarr *ssttrrccppyy(_s_1, _s_2)
       cchhaarr *_s_1, *_s_2;

       cchhaarr *ssttrrnnccppyy(_s_1, _s_2, _n)
       cchhaarr *_s_1, *_s_2;

       iinntt ssttrrlleenn(_s)
       cchhaarr *_s;

       cchhaarr *iinnddeexx(_s, _с)
       cchhaarr *_s, _с;

       cchhaarr *rriinnddeexx(_s, _с)
       cchhaarr *_s;

       ttaabbiiffyy(_s, _d)
       cchhaarr *_s, *_d;

ООППИИССААННИИЕЕ
       Эти функции работают с завершающимися нулевыми симво-
       лами строками. При заполнении строк контроль перепол-
       нения не производиться.

       Функция ssttrrccaatt добавляет строку _s_2 в конец строки _s_1.
       Функция  ssttrrnnccaatt  производит аналогичные действия, но
       копирует не более _n символов. Обе функции  возвращают
       указатель  на  результирующую  строку,  завершающуюся
       нулевым символом.

       Функция ssttrrccmmpp сравнивает свои аргументы и возвращает
       целое число, которое больше, равно или меньше нуля, в
       зависимости от того, больше, равно или меньше _s_1  чем


                            -1-                     ДЕМОС/PV


STRING(3)       ДЕМОС. Библиотечные функции        STRING(3)


       _s_2.   Сравнение производится в соответствии со значе-
       ниями кодов символов.  Функция ssttrrnnccmmpp выполняет ана-
       логичное  сравнение,  но  рассматривает  при  этом не
       более _n символов.   Функции  ssttrrllccmmpp  и  ssttrrnnllccmmpp,  в
       отличие  от  вышеописанных,  производят  сравнение  в
       соответствии с лексикографическим порядком букв  анг-
       лийского и русского алфавитов.

       Функция ssttrrccppyy записывает строку _s_2 в _s_1,  останавли-
       ваясь   после  пересылки  нулевого  символа.  Функция
       ssttrrnnccppyy  пересылает  точно  _n  символов,  усекая  или
       заполняя  нулевыми  символами  строку  _s_2. Если длина
       строки _s_2 равна _n или более, строка-приемник может не
       завершаться   нулевым   символом.   В  обоих  случаях
       результат помещается в _s_1.

       Функция ssttrrlleenn возвращает число ненулевых символов  в
       строке _s.

       Функция iinnddeexx (rriinnddeexx) возвращает  указатель  первого
       (последнего) появления символа _c в строке _s или нуль,
       если данного символа в строке нет.

       Функция ttaabbiiffyy преобразует  свой  первый  аргумент  в
       строку  (второй аргумент), в которой, где только воз-
       можно, большие количества пробелов заменены на  табу-
       ляции.   Это   дает  более  компактное  представление
       строки.
































                            -2-                     ДЕМОС/PV

