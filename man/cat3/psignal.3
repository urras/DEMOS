
PSIGNAL(3)      ДЕМОС. Библиотечные функции       PSIGNAL(3)


ИИММЯЯ
       ppssiiggnnaall, ssyyss__ssiigglliisstt - название сигнала.

ФФООРРММААТТ
       ppssiiggnnaall(_s_i_g, _s)
       uunnssiiggnneedd _s_i_g;
       cchhaarr *_s;
       cchhaarr *ssyyss__ssiigglliisstt[];

ООППИИССААННИИЕЕ
       Функция ppssiiggnnaall выдает в стандартный файл диагностики
       сообщение,  содержащее строку, заданную аргументом _s,
       двоеточие, символьное обозначение сигнала номер _s_i_g и
       код  новой  строки.  Как правило, в качестве значения
       строки передается имя программы, при выполнении кото-
       рой был получен сигнал.

       Другой способ получения имени сигнала по его номеру -
       это  использование массива ssyyss__ssiigglliisstt, номер сигнала
       здесь  используется  как  индекс  таблицы  символьных
       имен.   Имя  выдается  без  завершаюшего  кода  конца
       строки.  Общее число сигналов, записанных в  таблице,
       определяется  макропеременной  NNSSIIGG из <_s_i_g_n_a_l._h>.  В
       программах полезно проверять значение  сигнала,  пос-
       кольку  возможно  появление сигналов, уже внесенных в
       систему, но еще не зарегистрированных в таблице.

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _s_i_g_n_a_l(2), _p_e_r_r_o_r(3)































                            -1-                     ДЕМОС/PV

