
PERROR(3)       ДЕМОС. Библиотечные функции        PERROR(3)


ИИММЯЯ
       ppeerrrroorr, ssyyss__eerrrrlliisstt, ssyyss__rreerrrrlliisstt, ssyyss__nneerrrr - сообще-
       ния об ошибках.

ФФООРРММААТТ
       ppeerrrroorr(_s)
       cchhaarr *_s;

       iinntt ssyyss__nneerrrr;
       cchhaarr *ssyyss__eerrrrlliisstt[];
       cchhaarr *ssyyss__rreerrrrlliisstt[];

ООППИИССААННИИЕЕ
       Функция ppeerrrroorr выдает в стандартный файл вывода  оши-
       бок  сообщение, описывающее последнюю ошибку, которая
       возникла во время обращения к системе из программы на
       языке  Си. Вначале печатается строка _s, затем двоето-
       чие, сообщение и символ новой строки. Полезнее  всего
       в  качестве  строки-аргумента  использовать имя прог-
       раммы, вызвавшей ошибку. Номер ошибки  содержится  во
       внешней переменной eerrrrnnoo, которая устанавливается при
       возникновении ошибки и не рассматривается при безоши-
       бочных вызовах.

       Для упрощения форматирования  сообщений  используется
       массив  строк  сообщений  ssyyss__eerrrrlliisstt (диагностики на
       английском языке) или  ssyyss__rreerrrrlliisstt  (диагностики  на
       русском  языке,  см.   _e_d_i_a_g(3));  можно использовать
       значение eerrrrnnoo как индекс в этой таблице, чтобы полу-
       чить  строку  сообщения  без  символа  новой  строки.
       ssyyss__nneerrrr - число сообщений в таблице. Это число нужно
       проверить,  так как в систему могут добавляться новые
       сообщения прежде, чем они будут добавлены в таблицу.

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _i_n_t_r_o(2), _p_s_i_g_n_a_l(3)
























                            -1-                     ДЕМОС/PV

