
NLIST(3)        ДЕМОС. Библиотечные функции         NLIST(3)


ИИММЯЯ
       nnlliisstt - выборка адреса переменной по имени.

ФФООРРММААТТ
       #iinncclluuddee <_a._o_u_t._h>

       nnlliisstt(_f_i_l_e_n_a_m_e, _n_l)
       cchhaarr *_f_i_l_e_n_a_m_e;
       ssttrruucctt nnlliisstt _n_l[];

ООППИИССААННИИЕЕ
       Функция nnlliisstt рассматривает  список  имен  переменных
       файла _f_i_l_e_n_a_m_e и выборочно составляет список их адре-
       сов.  Список имен состоит из массива структур, содер-
       жащих  имена,  типы  и  значения.  Список завершается
       нулевым именем.  Каждое  имя  ищется  в  списке  имен
       переменных  файла.  Если имя найдено, в следующие два
       поля записываются тип и значение. Если  переменной  с
       таким  именем  нет, оба входа устанавливаются в нуль.
       Описание структуры nnlliisstt приведено в _a._o_u_t(5).

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _a._o_u_t(5)

ДДИИААГГННООССТТИИККАА
       Входы всех типов устанавливаются в 0, если  указанный
       файл не обнаружен или неверен список имен.

































                            -1-                     ДЕМОС/PV

