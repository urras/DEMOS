
SIGNAL(3F)    ДЕМОС. Руководство программиста     SIGNAL(3F)


ИИММЯЯ
       ssiiggnnaall - управление обработкой сигналов.

ФФООРРММААТТ
       iinntteeggeerr ffuunnccttiioonn ssiiggnnaall (_s_i_g, _p_r_o_g, _f_l_a_g)
       iinntteeggeerr _s_i_g, _f_l_a_g
       eexxtteerrnnaall _p_r_o_g

ООППИИССААННИИЕЕ
       Подпрограмма ssiiggnnaall является  фортранным  интерфейсом
       системного   вызова  _s_i_g_n_a_l(2)  и  предназначена  для
       управления обработкой сигналов.

       Параметр _s_i_g задает номер сигнала. Если значение _f_l_a_g
       меньше  нуля,  параметр  _p_r_o_g используется в качестве
       адреса  подпрограммы  обработки  указанного  сигнала.
       Значения 0 и 1 параметра _f_l_a_g служат, соответственно,
       для блокировки сигнала и  восстановления  стандартной
       для него реакции.

       После возврата переменная _f_l_a_g будет  соответствовать
       предыдущему значению реакции на сигнал, которое может
       использоваться  для  восстановления  прежнего  режима
       обработки.   В  случае  возникновения ошибки значение
       переменной _f_l_a_g будет меньше нуля.

ФФААЙЙЛЛЫЫ
       /_u_s_r/_l_i_b/_l_i_b_U_7_7

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _k_i_l_l(1), _s_i_g_n_a_l(2), _k_i_l_l(3f)

ЗЗААММЕЕЧЧААННИИЯЯ
       Подпрограммы стандартной библиотеки  компилятора  ff7777
       при  старте  программы заказывают обработку некоторых
       сигналов.  Для корректного восстановления  необходимо
       сохранять значение параметра _f_l_a_g после первого обра-
       щения к ssiiggnnaall.

       При вызове подпрограммы обработки сигнала  устанавли-
       вается  стандартный  для системы режим его обработки.
       Если это не устраивает, необходимо повторно  вызывать
       ssiiggnnaall из подпрограммы обработки.

       Подпрограмме обработки передается один параметр  типа
       iinntteeggeerr,  значение  которого равно номеру полученного
       сигнала.













                            -1-                     ДЕМОС/PV

