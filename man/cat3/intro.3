
INTRO(3)        ДЕМОС. Библиотечные функции         INTRO(3)


ИИММЯЯ
       iinnttrroo - введение в раздел "_Б_и_б_л_и_о_т_е_ч_н_ы_е _ф_у_н_к_ц_и_и".

ООППИИССААННИИЕЕ
       В данной части приводятся описания библиотечных функ-
       ций  ДЕМОС.   Название каждого из разделов состоит из
       имени функции и индекса, указывающего на тематическую
       принадлежность этой функции.  Индекс может иметь одно
       из следующих значений:

       (_3)  Эти функции совместно с системными вызовами (см.
            раздел 2) и функциями, относящимися к подразделу
            _3_s, содержатся в  стандартной  библиотеке  _l_i_b_c,
            которая  автоматически подключается при трансля-
            ции с языков Си - _c_c(1) и Фортран-77  -  _f_7_7(1).
            Редактор связей lldd использует эту библиотеку при
            задании ключа --llcc.

       (_3_f) Функции,  доступные   из   программ   на   языке
            Фортран-77.  При использовании ff7777 они автомати-
            чески (если необходимо) загружаются  из  библио-
            теки.  При работе с редактором связей эти библи-
            отеки подключаются с помощью ключей --llFF7777, --llII7777
            и --llUU7777.

       (_3_m) Функции  математической  библиотеки  _l_i_b_m.   Эта
            библиотека автоматически подключается при транс-
            ляции с языка Фортран-77.   Редактор  связей  lldd
            использует эту библиотеку при задании ключа --llmm.
            Определения для  использования  этих  функций  в
            языке Си содержатся в файле <_m_a_t_h._h>.

       (_3_s) Функции  стандартной  библиотеки   ввода/вывода.
            Входят в состав библиотеки _l_i_b_c.  Общее описание
            приводится в разделе _s_t_d_i_o(3s).  Определения для
            этих функций содержаться в файле <_s_t_d_i_o._h>.

       (_3_x) Различные функции, не входящие  в  состав  стан-
            дартных  библиотек  ДЕМОС.  Имена  библиотек,  в
            которые они входят, приводятся в конкретных опи-
            саниях.

       (_3_c) Функции, включенные для совместимости с  другими
            ОС семейства.

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _n_m(1), _l_d(1), _c_c(1), _f_7_7(1), _i_n_t_r_o(2).

ДДИИААГГННООССТТИИККАА
       Функции математической библиотеки (_3_m) могут  возвра-
       щать состояние ошибки, если данная функция не опреде-
       лена для заданного аргумента, или когда  значение  не
       определено.  В  этом  случае  переменной  eerrrrnnoo  (см.
       _i_n_t_r_o(2)) присваивается значение EEDDOOMM или EERRAANNGGEE.






                            -1-                        ДЕМОС

