
GETLOGIN(3)     ДЕМОС. Библиотечные функции      GETLOGIN(3)


ИИММЯЯ
       ggeettllooggiinn - получить регистрационное имя.

ФФООРРММААТТ
       cchhaarr *ggeettllooggiinn();

ООППИИССААННИИЕЕ
       Функция ggeettllooggiinn возвращает указатель на  регистраци-
       онное  имя, находящееся в файле /_e_t_c/_u_t_m_p.  Эта функ-
       ция в сочетании с ggeettppwwnnaamm может  использоваться  для
       определения  места  нужной  записи  в  файле паролей,
       когда  один  и  тот  же  идентификатор   пользователя
       используется несколькими регистрационными именами.

       Если ggeettllooggiinn вызывается процессом, для которого фай-
       лом стандартного вывова не является терминал, возвра-
       щается значение NNUULLLL.  Правильная процедура для опре-
       деления  регистрационного  имени  заключается  в том,
       чтобы вначале вызвать ggeettllooggiinn  и,  если  этот  вызов
       окажется неудачным, вызвать ggeettppwwiidd.

ФФААЙЙЛЛЫЫ
       /_e_t_c/_u_t_m_p

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _g_e_t_p_w_e_n_t(3), _g_e_t_g_r_e_n_t(3), _u_t_m_p(5), _g_e_t_p_w(3)

ДДИИААГГННООССТТИИККАА
       Если имя  не  обнаруживается,  возвращается  значение
       NNUULLLL.

ЗЗААММЕЕЧЧААННИИЯЯ
       Возвращаемые значения указывают на  статические  дан-
       ные,  содержимое  которых  изменяется  после  каждого
       вызова.

























                            -1-                     ДЕМОС/PV

