
WRITE(2)          ДЕМОС. Системные вызовы           WRITE(2)


ИИММЯЯ
       wwrriittee - запись в файл.

ФФООРРММААТТ
       wwrriittee(_f_d, _b_u_f, _n_b_y_t_e_s)
       cchhaarr *_b_u_f;
       iinntt  _f_d, _n_b_y_t_e_s;

ООППИИССААННИИЕЕ
       Системный вызов wwrriittee  записывает  _n_b_y_t_e_s  данных  из
       массива _b_u_f в файл с дескриптором _f_d.

       Информация записывается в файл по текущему  указателю
       чтения/записи.   После  завершения  обмена  указатель
       увеличивается на число записанных байт.

       Если у файла  установлен  режим  OO__AAPPPPEENNDD  (установку
       режимов файла см. _o_p_e_n(2) или _i_o_c_t_l(2)), то указатель
       перед операцией записи всегда устанавливается в конец
       файла.

       Для некоторых файлов значение указателя чтения/записи
       не  имеет  смысла  (например,  для терминала), тем не
       менее данные передаются.

       Если у FFIIFFOO файла установлен режим OO__NNDDEELLAAYY, то  про-
       изводится обмен без ожидания. Т.е. если во время сис-
       темной фазы обработки этого вызова потребуется ожида-
       ние  (например,  пишем  в файл, а он заполнен; у FFIIFFOO
       файлов есть ограничение на длину - параметр PPIIPPSSIIZZ  в
       include-файле <_s_y_s/_i_n_o_d_e._h>), то будет сделан возврат
       из системного вызова без записи.

       Если  коды  доступа  файла,  в  который  записывается
       информация, содержит признак _s_e_t-_U_I_D, и процесс рабо-
       тает в  непривилегированном  режиме,  данный  признак
       удаляется (в целях защиты информации).

       Если у файла установлен режим OO__SSYYNNCC, то  при  записи
       каждый раз будет ожидаться конец действительно произ-
       веденной записи.

ЗЗННААЧЧЕЕННИИЕЕ
       При успешном завершении возвращается число записанных
       байт, в случае ошибки - значение -1.

ККООДДЫЫ ООШШИИББООКК
       При ошибке переменная eerrrrnnoo может принимать следующие
       значения:

       [[EEBBAADDFF]]     Указанный дескриптор не является дескрип-
                   тором файла, открытого для записи.

       [[EEPPIIPPEE]]     Попытка  записи  в  межпроцессный  канал,
                   который никто не читает.

       [[EEFFBBIIGG]]     При записи в файл превышается  допустимый
                   размер файла.


                            -1-                     ДЕМОС/PV


WRITE(2)          ДЕМОС. Системные вызовы           WRITE(2)


       [[EEQQUUOOTT]]     При записи в файл  превышаются  ограниче-
                   ния,  установленные  для  данной файловой
                   системы.

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _c_r_e_a_t(2), _i_o_c_t_l(2), _l_s_e_e_k(2), _o_p_e_n(2), _p_i_p_e(2)






















































                            -2-                     ДЕМОС/PV

