
MOUNT(8)        ДЕМОС. Сопровождение системы        MOUNT(8)


ИИММЯЯ
       mmoouunntt, uummoouunntt - примонтировать (отмонтировать) файло-
       вую систему.

ФФООРРММААТТ
       //eettcc//mmoouunntt [--pp [--tt _t_m_p_f_i_l_e]] [_s_p_e_c_i_a_l [_n_a_m_e] [--rr]]
       //eettcc//mmoouunntt [--pp[_д_и_а_п] [--tt _t_m_p_f_i_l_e]] --aa

       //eettcc//uummoouunntt _s_p_e_c_i_a_l
       //eettcc//uummoouunntt [[--pp[_д_и_а_п]] [--aa]

ООППИИССААННИИЕЕ
       Команда mmoouunntt монтирует на уже  существующий  каталог
       _n_a_m_e  файловую  систему, расположенную на устройстве,
       доступ к которому  осуществляется  через  специальный
       файл  _s_p_e_c_i_a_l.   Если каталог _n_a_m_e был не пустым, его
       содержимое будет временно недоступно.  Ключ --rr служит
       для  подключения  файловой системы только для чтения.
       Ключ --pp служит для проверки  файловой  системы  перед
       монтированием с помощью команды //eettcc//ffsscckk --pp.

       При использовании ключа --aa после ключа --pp можно  ука-
       зывать  номера  проверяемых файловых систем (аргумент
       _д_и_а_п) по файлу /_e_t_c/_f_s_t_a_b.  Можно указать  начиная  с
       какого  номера  проверять файловые системы (например,
       --pp2 или --pp2-), или диапазон номеров (например, --pp2-4)
       или  каким  номером закончить проверку (например, --pp-
       3).  Если ничего не указано, проверяются все файловые
       системы.

       Ключ --tt _t_m_p_f_i_l_e передается команде //eettcc//ffsscckk для ука-
       зания  имени  рабочего  файла в случае, если файловая
       система достаточно велика. Если имя каталога _n_a_m_e  не
       указано,  то  оно определяется из суперблока файловой
       системы и она монтируется на тот же  каталог,  что  и
       при предыдущем монтировании.

       Если команда mmoouunntt вызывается с ключом --aa, то  монти-
       рование  выполняется  для  всех  стандартных файловых
       систем, описанных в файле /_e_t_c/_f_s_t_a_b.  При  этом  имя
       файловой  системы  "?"  служит для указания того, что
       его нужно  прочитать  из  суперблока.  Это  позволяет
       использовать  сменные  носители  информации,  которые
       автоматически монтируются на свое место.

       Команда uummoouunntt отключает файловую  систему,  располо-
       женную  на  устройстве _s_p_e_c_i_a_l и освобождает каталог,
       на который она была смонтирована.

       Если команда uummoouunntt вызывается с ключом --aa, то отмон-
       тирование выполняется для всех файловых систем, запи-
       санных в файле /_e_t_c/_m_t_a_b, в обратном порядке.

       Если команда uummoouunntt вызывается с ключом --pp, то отмон-
       тирование выполняется для всех файловых систем, запи-
       санных в файле /_e_t_c/_f_s_t_a_b, в обратном  порядке.   При
       ключе  --pp может быть указан диапазон, так же, как и в
       mmoouunntt.


                            -1-                     ДЕМОС/PV


MOUNT(8)        ДЕМОС. Сопровождение системы        MOUNT(8)


       Обе команды поддерживают таблицу смонтированных  фай-
       ловых  систем,  которая  хранится  в файле /_e_t_c/_m_t_a_b.
       Команда  mmoouunntt,  вызванная  без  параметров,   выдает
       содержимое этой таблицы.

       Файловые системы, расположенные на физически защищен-
       ных по записи внешних устройствах, и файловые системы
       на магнитных лентах рекомендуется монтировать  только
       на чтение.

ФФААЙЙЛЛЫЫ
       /_e_t_c/_m_t_a_b   таблица монтирования
       /_e_t_c/_f_s_t_a_b  таблица стандартных файловых систем

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _m_o_u_n_t(2), _m_t_a_b(5), _f_s_t_a_b(5)

ЗЗААММЕЕЧЧААННИИЯЯ
       Монтирование некорректной файловой системы может при-
       вести к аварии системы.








































                            -2-                     ДЕМОС/PV

