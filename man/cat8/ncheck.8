
NCHECK(8)       ДЕМОС. Сопровождение системы       NCHECK(8)


ИИММЯЯ
        nncchheecckk - выдать имена файлов по I-номерам.

ФФООРРММААТТ
        nncchheecckk [--ii _н_о_м_е_р_а] [--aa] [--ss] _ф_а_й_л_о_в_а_я-_с_и_с_т_е_м_а...

ООППИИССААННИИЕЕ
        Команда nncchheecckk выдает список полных  имен  файлов  и
        I-номеров  для  всех  указанных  файловых систем.  К
        именам  каталогов  добавляется  суффикс   "/.".    В
        командной   строке  могут  использоваться  следующие
        ключи:

        --ii      Ограничивает вывод только теми файлами,  для
                которых указаны I-номера.

        --aa      Задает режим выдачи всех имен, в этом режиме
                распечатываются  имена  "."  и "..", которые
                обычно подавляются.

        --ss      Ограничивает вывод только специальными  фай-
                лами и файлами с режимом установки идентифи-
                кации пользователя (режим  предназначен  для
                выявления скрытых нарушений защиты).

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
        _i_c_h_e_c_k(8), _s_o_r_t(1), _f_s_c_k(8)

ДДИИААГГННООССТТИИККАА
        Если структура файловой системы неверна, "??"  обоз-
        начает  предка  для файлов без предков, а полное имя
        файла, начинающееся с "..." обозначает цикл.

ЗЗААММЕЕЧЧААННИИЯЯ
        При работе с файловыми системами, как правило,  дос-
        таточно команды _f_s_c_k(8).

        Выводимые данные не упорядочены и, вероятно,  должны
        быть отсортированы.





















                            -1-                     ДЕМОС/PV

