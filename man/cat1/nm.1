
NM(1)                  ДЕМОС. Команды                  NM(1)


ИИММЯЯ
        nnmm - выдать список имен.

ФФООРРММААТТ
        nnmm [--ggnnoopprruu] [_ф_а_й_л...]

ООППИИССААННИИЕЕ
        Команда nnmm распечатывает список имен (таблицу симво-
        лов)   каждого  из  указанных  в  списке  параметров
        объектных файлов. Если параметр  представляет  собой
        архив,  будет  распечатываться  таблица символов для
        каждого объектного файла архива. Если _ф_а_й_л  не  ука-
        зан, распечатываются символы из файла _a._o_u_t.

        Каждому символическому имени предшествует его значе-
        ние (пробелы, если оно не определено) и одна из букв
        UU (неопределенное), AA (абсолютное), TT (символ  текс-
        тового  сегмента),  DD  (символ  сегмента  данных), BB
        (символ сегмента _b_s_s) или  CC  (общий  символ).  Если
        символ  является  локальным (не внешним), то печата-
        ется соответствующая строчная  буква.  По  умолчанию
        вывод сортируется по алфавиту.

        Можно использовать следующие ключи:

        --gg      Выдавать только  глобальные  (внешние)  сим-
                волы.

        --nn      Отсортировать по значениям, а  не  по  алфа-
                виту.

        --oo      Указывать имя файла или  элемента  архива  в
                каждой строке вывода, а не только один раз.

        --pp      Не сортировать, печатать в  порядке  таблицы
                символов.

        --rr      Отсортировать в обратном порядке.

        --uu      Печатать только неопределенные символы.

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
        _a_r(1), _a_r(5), _a._o_u_t(5)

















                            -1-                     ДЕМОС/PV

