
IDENT(1)               ДЕМОС. Команды               IDENT(1)


ИИММЯЯ
       iiddeenntt - идентификация файлов.

ФФООРРММААТТ
       iiddeenntt _ф_а_й_л ...

ООППИИССААННИИЕЕ
       Команда iiddeenntt проверяет указанные  файлы  на  наличие
       строк   вида  $$_k_e_y_w_o_r_d: ... $$,  заполняемых  системой
       ведения версий rrccss(1), где _k_e_y_w_o_r_d является одним  из
       следующих слов:

       AAuutthhoorr    - автор версии
       DDaattee      - дата записи версии
       HHeeaaddeerr    - заголовок версии
       LLoocckkeerr    - владелец версии
       LLoogg       - заголовок комментария версии
       RReevviissiioonn  - номер версии
       SSoouurrccee    - исходный текст версии
       SSttaattee     - статус версии

       Эти конструкции обычно вставляются в файл пользовате-
       лем.  В результате работы команды ccoo(1) после каждого
       параметра  записывается  его  значение  (имя  автора,
       номер  версии  и  т.д.).   Команда iiddeenntt осуществляет
       поиск таких строк и  выдает  их  в  стандартный  файл
       вывода.

       Команда iiddeenntt с равным успехом  может  использоваться
       для идентификации как текстовых, так и объектных фай-
       лов.  Например, если программа, написанная  на  языке
       Си, которая находится в файле _f._c, содержит текстовую
       константу вида:

       cchhaarr _r_c_s_i_d[] = "$$HHeeaaddeerr::  _H_e_a_d_e_r _i_n_f_o_r_m_a_t_i_o_n $$";

       то после компиляции этого файла в файл _f._o и выполне-
       ния команды:

              iiddeenntt  _f._c  _f._o

       будет выдано:

       _f._c:
               $$HHeeaaddeerr::  _H_e_a_d_e_r _i_n_f_o_r_m_a_t_i_o_n $$
       _f._o:
               $$HHeeaaddeerr::  _H_e_a_d_e_r _i_n_f_o_r_m_a_t_i_o_n $$

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _c_i(1),  _c_o(1),   _r_c_s(1),   _r_c_s_d_i_f_f(1),   _r_c_s_i_n_t_r_o(1),
       _r_c_s_m_e_r_g_e(1), _r_l_o_g(1), _r_c_s_f_i_l_e(5)









                            -1-                     ДЕМОС/PV

