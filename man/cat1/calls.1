
CALLS(1)               ДЕМОС. Команды               CALLS(1)


ИИММЯЯ
       ccaallllss - построение графа вызовов функций в программах
       на Си.

ФФООРРММААТТ
       ccaallllss [_к_л_ю_ч_и...] [_ф_а_й_л_ы...]

ООППИИССААННИИЕЕ
       Команда  ccaallllss  предназначена  для  построения  схемы
       вызовов  функций  в программах на языке Си.  Строится
       иерархическая таблица, в которой для  каждой  функции
       указывается,  какие  функции она вызывает, а также из
       каких функций она вызывается.   Команда  ccaallllss  может
       быть  полезна  при  исследовании  логики  программ на
       языке Си.

ККЛЛЮЮЧЧИИ
       --tt     Сжатая форма выдачи (используется  по  умолча-
              нию).  Если  какая-либо  функция вызывается из
              нескольких мест, то все повторные  использова-
              ния   будут  обозначаться  просто  ссылкой  на
              строку, где описано первое использование.

       --vv     Расширенная  форма   выдачи.   Все   повторные
              использования   функций  распечатываются  пол-
              ностью.

       --ww _N_N  Задает максимальную ширину листа в _N_N  позиций
              (по умолчанию 132).

       Указанные файлы предварительно обрабатываются препро-
       цессором  /_l_i_b/_c_p_p.   Можно  указать ключи препроцес-
       сору:

       --DD_d_e_f  Эквивалентно ##ddeeffiinnee _d_e_f в тексте программы.

       --UU_d_e_f  Эквивалентно ##uunnddeeff _d_e_f в тексте программы.

       --II_i_n_c  Указывает, что _i_n_c_l_u_d_e-файлы  нужно  искать  в
              каталоге _i_n_c.

       --NN     Указывает,  что  отменяется  обработка   файла
              препроцессором  /_l_i_b/_c_p_p.  Это бывает полезно,
              если  к  исследуемой  программе   не   хватает
              _i_n_c_l_u_d_e-файлов.    Конструкции   препроцессора
              iiffddeeff, iiff, ddeeffiinnee, iinncclluuddee не обрабатываются.

       --ff _ф_у_н_к_ц_и_я
              Выдача дерева вызовов  будет  сделана  не  для
              всех  функций, а лишь для указанных.  При этом
              файлы надо указывать не в командной строке,  а
              подавать  программе ccaallllss на стандартный ввод,
              например:

              ccaallllss --ff _f_u_n_c_1 --ff _f_u_n_c_2 < _f_i_l_e._c

       --VV     Принуждает  программу  выдавать  имя  текущего
              обрабатываемого файла.


                            -1-                     ДЕМОС/PV


CALLS(1)               ДЕМОС. Команды               CALLS(1)


ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _c_c(1), _c_x_r_e_f(1)


























































                            -2-                     ДЕМОС/PV

