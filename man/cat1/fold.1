
FOLD(1)                ДЕМОС. Команды                FOLD(1)


ИИММЯЯ
       ffoolldd - разбиение длинных строк.

ФФООРРММААТТ
       ffoolldd [--_l_e_n_g_t_h] [_ф_а_й_л...]

ООППИИССААННИИЕЕ
       Команда ffoolldd разбивает каждую строку,  которая  имеет
       длину  больше  _l_e_n_g_t_h,  на  несколько строк.  Если не
       задан параметр _ф_а_й_л, обрабатываются строки  из  стан-
       дартного   файла  ввода.   По  умолчанию  разбиваются
       строки, содержащие  более  80-ти  символов.   Если  в
       обрабатываемом тексте могут встречаться символы табу-
       ляции, величина _l_e_n_g_t_h должна быть  кратной  8.   Это
       ограничение можно обойти, если предварительно развер-
       нуть табуляции в пробелы с помощью команды eexxppaanndd(1).

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _e_x_p_a_n_d(1)

ЗЗААММЕЕЧЧААННИИЯЯ
       Если файл содержит подчеркивание при помощи возвратов
       на  шаг (например, в выдаче nnrrooffff), могут быть непри-
       ятности при свертывании.




































                            -1-                     ДЕМОС/PV

