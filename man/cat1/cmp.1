
CMP(1)                 ДЕМОС. Команды                 CMP(1)


ИИММЯЯ
       ccmmpp - сравнение двух файлов.

ФФООРРММААТТ
       ccmmpp [--ll] [--ss] _ф_а_й_л_1 _ф_а_й_л_2

ООППИИССААННИИЕЕ
       Два указанных файла сравниваются (если параметр _ф_а_й_л_1
       представляет собой '--', используется стандартный файл
       ввода). Без параметров команда ccmmpp не выдает  никаких
       сообщений,  если файлы одинаковы, или указывает номер
       байта и строки, где произошло несовпадение, если  они
       отличаются. Если один из файлов является началом дру-
       гого, об этом сообщается.

       --ll  Печатает номер байта (десятичный) и  отличающиеся
           байты (восьмеричные) для каждого несовпадения.

       --ss  Для отличающихся  файлов  ничего  не  печатается,
           возвращается только код.

ДДООППООЛЛННИИТТЕЕЛЛЬЬННЫЫЕЕ ССССЫЫЛЛККИИ
       _d_i_f_f(1), _c_o_m_m(1)

ДДИИААГГННООССТТИИККАА
       Возвращается выходной код, равный  0  для  идентичных
       файлов, 1 для отличающихся файлов и 2 для недоступных
       или пропущенных параметров.
































                            -1-                     ДЕМОС/PV

