
BAS(1)                 ДЕМОС. Команды                 BAS(1)


ИИММЯЯ
       bbaass - интерпретатор языка BASIC.

ФФООРРММААТТ
       bbaass [_ф_а_й_л]

ООППИИССААННИИЕЕ
       Команда bbaass является интерпретатором одного  из  диа-
       лектов  языка  BASIC.  Если в командной строке задано
       имя файла _ф_а_й_л, то перед вызовом интерпретатора  этот
       файл  считывается  (предполагается,  что  он содержит
       программу на языке BASIC). Интерпретатор  bbaass  допус-
       кает строки следующего вида:

       _о_п_е_р_а_т_о_р
       _м_е_т_к_а _о_п_е_р_а_т_о_р

       В качестве  метки  используется  целое  положительное
       число. Помеченные операторы запоминаются для последу-
       ющего выполнения и сортируются в порядке  возрастания
       значений  меток.  Непомеченные  операторы выполняются
       немедленно. Если оператор содержит вычисляемое  выра-
       жение без присваивания, результат выдается сразу.

       ССИИННТТААККССИИСС ООППЕЕРРААТТООРРООВВ

       _в_ы_р_а_ж_е_н_и_е
              Выражение выполняется для достижения желаемого
              эффекта  (присваивание  или вызов функции) или
              для печати, как было сказано выше. В  дальней-
              шем выражение будет обозначаться символом _V.

       ccoommmmeenntt
       Этот оператор игнорируется, применяется для включения
       комментария в программу.

       ddoonnee
       Выход из интерпретатора обратно в систему.

       dduummpp
       Вызывает печать имени и текущего значения всех  пере-
       менных в процессе выполнения программы.

       eeddiitt
       Вход в редактор eedd.  В качестве имени файла редактору
       eedd  передается параметр _ф_а_й_л.  После выхода из редак-
       тора происходит возврат в интерпретатор, причем прог-
       рамма,   находящаяся   в  файле  _ф_а_й_л,  компилируется
       заново.

       ffoorr _и_м_я = _V _V _о_п_е_р_а_т_о_р

       ffoorr _и_м_я = _V _V
       _г_р_у_п_п_а _о_п_е_р_а_т_о_р_о_в
       nneexxtt

              Оператор ffoorr порождает многократное выполнение
              оператора  (первая  форма  записи)  или группы


                            -1-                      ДЕМОС/P


BAS(1)                 ДЕМОС. Команды                 BAS(1)


              операторов (вторая форма записи),  контролируя
              количество  итераций  по  значению  переменной
              _и_м_я.  Этой переменной  присваивается  значение
              первого  выражения,  которое  увеличивается на
              единицу после каждого шага  выполнения  цикла.
              Цикл  продолжается  до  тех пор, пока значение
              переменной не превысит значения второго  выра-
              жения.

       ggoottoo _V Выражение вычисляется,  переводится  в  целое,
              после  чего  происходит  переход  к оператору,
              метка  которого  равна   значению   выражения.
              Программа   продолжает   выполняться  с  этого
              места.  Если данный оператор  не  помечен,  то
              перед  его интерпретацией компилируются опера-
              торы, накопленные ранее.

       iiff _V _о_п_е_р_а_т_о_р

       iiff _V
       _г_р_у_п_п_а _о_п_е_р_а_т_о_р_о_в
       [ eellssee
       _г_р_у_п_п_а _о_п_е_р_а_т_о_р_о_в ]
       ffii

              Оператор (первая форма) или группа  операторов
              (вторая   форма)  выполняется,  если  значение
              выражения  не  равно  нулю.  Во  второй  форме
              допускается наличие альтернативной группы опе-
              раторов.

       lliisstt [_V [_V]]
              Этот оператор применяется для просмотра текста
              программы.   Если отсутствуют аргументы, выда-
              ется вся  программа.  Если  присутствует  один
              аргумент,  то  выдается  только один оператор,
              метка которого равна значению заданного  выра-
              жения.  Если заданы два аргумента, то выдаются
              операторы в указанном диапазоне.

       pprriinntt _с_п_и_с_о_к
              _с_п_и_с_о_к включает в себя выражения, переменные и
              текстовые   константы;   текстовые   константы
              берутся в двойные кавычки ("). Оператор  pprriinntt
              выдает  значения  перечисленных  выражений или
              переменных совместно с текстом.

       pprroommpptt _с_п_и_с_о_к
              Действует аналогично оператору  pprriinntt,  только
              после  выдачи последнего символа не происходит
              перехода на новую строку.

       rreettuurrnn [_V]
              Выражение вычисляется, и его значение  исполь-
              зуется в качестве значения, которое возвращает
              функция. Если аргумент отсутствует,  возвраща-
              ется ноль.



                            -2-                      ДЕМОС/P


BAS(1)                 ДЕМОС. Команды                 BAS(1)


       rruunn    Программа  компилируется,  датчику   случайных
              чисел   устанавливается   стартовое  значение.
              Управление передается оператору  с  наименьшим
              значением метки.

       ssaavvee [_V [_V]]
              Оператор ssaavvee  работает  аналогично  оператору
              lliisstt,  только  вывод  идет не на терминал, а в
              файл _ф_а_й_л,  заданный  при  вызове  bbaass.   Если
              команда   bbaass   была  вызвана  без  аргумента,
              используется файл _b._o_u_t в текущем каталоге.

       ССИИННТТААККССИИСС ВВЫЫРРААЖЖЕЕННИИЙЙ

       _и_д_е_н_т_и_ф_и_к_а_т_о_р
              Идентификатор  может  содержать   произвольное
              количество  символов,  однако значимыми счита-
              ются первые четыре. Идентификатор должен начи-
              наться  с  буквы  и  может  состоять только из
              латинских строчных букв и цифр.

       _ч_и_с_л_о  Число определяет числовую константу и  записы-
              вается  в  традиционном  фортрановском  стиле.
              Число состоит из цифр и может содержать  деся-
              тичную  точку или знак порядка ee с последующей
              экспонентой.  Например: 4, -15, 0.34, 1.2ee+5.

       ((_V))    Скобки применяются для  изменения  старшинства
              выполнения операций.

       ___V     Инверсия значения выражения.

       _V _о_п_е_р_а_ц_и_я _V
              Выполнение операции.  Полный  список  операций
              приводится ниже.

       _V(( [_V[, _V]] ))
              Вызов функции с  заданным  числом  аргументов.
              Значение   первого  выражения  используется  в
              качестве метки оператора, с  которого  начина-
              ется  функция. Если значение выражения получа-
              ется отрицательным, вызывается соответствующая
              встроенная  функция (правда, неизвестно, какая
              именно). Список встроенных функций  приводится
              ниже.

       _и_д_е_н_т_и_ф_и_к_а_т_о_р[[ _V[, _V]]]
              Значение  выражения,  переведенное  в   целое,
              используется  для  индексации массивов. Запись
              вида _a[[1,2]] равносильна записи  вида  _a[[1]][[2]].
              Значения  индексов усекаются до диапазона от 0
              до 32767.

       ССППИИССООКК ООППЕЕРРААЦЦИИЙЙ

       ==    Операция присваивания. Левый операнд должен быть
            идентификатором  или элементом массива. Значение
            правого операнда присваивается левому.


                            -3-                      ДЕМОС/P


BAS(1)                 ДЕМОС. Команды                 BAS(1)


       && ||  Операция _л_о_г_и_ч_е_с_к_о_е  _и  (&&)  дает  в  результате
            ноль, если какой-либо из операндов равен нулю, и
            не ноль,  если оба операнда не равны нулю.  Опе-
            рация _л_о_г_и_ч_е_с_к_о_е _и_л_и (||) дает в результате ноль,
            если оба операнда равны нулю, и  не  ноль,  если
            какой-либо из них не равен нулю.

       <<  <<==  >>  >>==  <<>>
            Операции отношения:

            <<      меньше;

            <<==     не больше;

            >>      больше;

            >>==     не меньше;

            <<>>     не равно;


       возвращают значение одного из аргументов, если  отно-
       шение  соблюдается,  в  противном случае возвращается
       ноль. Операции отношения одного уровня допускают вло-
       женность  следующего  вида: результат операции _a>>_b>>_c,
       будет аналогичен результату операции _a>>_b&&_b>>_c.

       ++ --  Сложение и вычитание.

       ** //  Умножение и деление.

       ^^    Возведение в степень.

       ВВССТТРРООЕЕННННЫЫЕЕ ФФУУННККЦЦИИИИ

       aarrgg(_i)  Получение _i-того  параметра  текущего  вызова
               функции.

       eexxpp(_x)  Экспонента от _x.

       lloogg(_x)  Натуральный логарифм от _x.

       ssqqrr(_x)  Квадратный корень от _x.

       ssiinn(_x)  Синус от _x (радианы).

       ccooss(_x)  Косинус от _x (радианы).

       aattnn(_x)  Арктангенс от _x.  Возвращает значения в  диа-
               пазоне от -_п_и/2 до _п_и/2

       rrnndd()   Датчик случайных чисел, возвращает значения в
               диапазоне от 0 до 1.

       eexxpprr()  Программный ввод. Строка  читается  из  стан-
               дартного  файла  ввода  и рассматривается как
               выражение.  Программа получает значение этого
               выражения.


                            -4-                      ДЕМОС/P


BAS(1)                 ДЕМОС. Команды                 BAS(1)


       aabbss()   Абсолютное значение.

       iinntt(_x)  Перевод _x в целое.

ФФААЙЙЛЛЫЫ
       /_t_m_p/_b_t_m*   временные файлы
       _b._o_u_t       файл для сохранения программы
       /_b_i_n/_e_d     редактор

ДДИИААГГННООССТТИИККАА
       Синтаксические ошибки приводят к выдаче  некорректной
       строки.
















































                            -5-                      ДЕМОС/P

