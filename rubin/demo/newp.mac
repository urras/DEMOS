\r
{type Вводим новую часть. Номера новых частей должны быть > существующего}
retrieve (макс_номер=max(части.номер))
\g\r
\mark start
append to части
(номер={read Номер? },имя="{read Имя? }",
цвет="{read Цвет? }", вес= {read Вес? },колич={read Колич? })
\v\a
\branch ?{read ЕЩЕ (1 - да, 0 - проверить и выполнять)? }=1 start
{type Проверьте результат и введите команду \\ g}
\v\a
\e
